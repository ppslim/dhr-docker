# app-fireflyiii

Deploys a `fireflyiii/core` instance

## `.env` required

Yes

## Secrets required

`APP_KEY`
`DB_PASSWORD`
`STATIC_CRON_TOKEN`

`MYSQL_ROOT_PASSWORD` Defined but not required


## Volumes required

Shared volume is defined in compose
